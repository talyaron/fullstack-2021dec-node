"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const child_process_1 = require("child_process");
const path_1 = require("path");
function getHelp(commandName) {
    const command = `${process.argv[0]} ${path_1.join(__dirname, '../../bin/run')} ${commandName} help`;
    const help = child_process_1.execSync(command)
        .toString('utf8')
        .trim();
    if (!help) {
        throw new Error(`The help string is unexpectedly empty: '${command}'`);
    }
    return help;
}
exports.getHelp = getHelp;
//# sourceMappingURL=get-help.js.map